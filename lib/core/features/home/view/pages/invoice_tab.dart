import 'package:flutter/cupertino.dart';

class InvoiceTab extends StatelessWidget {
  const InvoiceTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Invoice Tab'),
    );
  }
}
